import { EqualsValidatorDirective } from './equals-validator.directive';

describe('EqualsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
